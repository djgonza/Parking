var mapa = new MapaSeleccionPlazas(document.getElementById('mapa'));

mapa.setPlaza (234, 285, 0, 61, 26, 0, "f14", false, "#8C6239");
mapa.setPlaza (1214, 635, 0, 26, 103, 0, "f14", false, "#8C6239");

/*var el = document.getElementById('mapa'); 

el.addEventListener("touchstart", handleStart, false);
el.addEventListener("touchend", handleEnd, false);
el.addEventListener("touchcancel", handleCancel, false);
el.addEventListener("touchleave", handleEnd, false);
el.addEventListener("touchmove", handleMove, false);

function handleStart(event) {
  // Handle the start of the touch
  //console.log(event);
}

function handleEnd(event) {
  // Handle the start of the touch
  //console.log(event);
}

function handleCancel(event) {
  // Handle the start of the touch
  //console.log(event, "cancel");
}

function handleMove(event) {
  // Handle the start of the touch
  console.log(event, event.touches[0].clientX, event.touches[0].clientY, "move");


}*/